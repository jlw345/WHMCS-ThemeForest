/*

Project     : DAdmin - Responsive Bootstrap HTML Admin Dashboard
Version     : 1.0
Author      : ThemeLooks
Author URI  : https://themeforest.net/user/themelooks

*/

(function ($) {
    "use strict";
    
    var $doc = angular.module('doc', []);

    $doc.directive("ngCustomHtml", function () {
        return function ( $$, $el ) {
            $el.append( $$.panel.content );

            Prism.highlightAllUnder( $el[0] );
        };
    });

    $doc.controller('doc', function ($scope) {
        $scope.doc = docData;
    });

}(jQuery));
